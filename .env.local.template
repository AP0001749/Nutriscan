# NutriScan — Local environment template
# Copy this to `.env.local` and replace placeholder values with your API keys and chosen models.

###############################
# Hugging Face (Vision / OCR)
###############################
# Your Hugging Face API key used for the Inference API (if required)
HUGGINGFACE_API_KEY=YOUR_HUGGINGFACE_API_KEY

# Publicly-available vision/captioning models that work with the hosted HF inference API.
# Recommended (small, reliable):
HUGGINGFACE_MODEL=Salesforce/blip-image-captioning-base

# Higher-accuracy option (larger model — may require more resources):
# HUGGINGFACE_MODEL=Salesforce/blip2-opt-2.7b

# Optional: leave blank unless you have a custom endpoint
# HUGGINGFACE_API_URL=https://api-inference.huggingface.co

###############################
# Google Gemini (Generative Language)
###############################
# Use the free-tier "flash" model by default (works with Google AI Studio free keys):
GEMINI_MODEL=gemini-1.5-flash

# Prefer v1 for Gemini Vision (v1beta may not have vision-enabled models anymore):
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1

# Server-side key (recommended) — set this if you want server-to-Google auth using an API key
GEMINI_API_KEY_SERVER=YOUR_GOOGLE_SERVER_API_KEY

# Alternative keys the code checks for (less secure if exposed to client):
# GEMINI_API_KEY=YOUR_LESS_SECURE_KEY
# NEXT_PUBLIC_GEMINI_API_KEY=YOUR_PUBLIC_KEY_IF_NEEDED

# You can supply a comma-separated model preference list if you want to try specific order:
# GEMINI_MODEL=gemini-1.5-pro,gemini-1.5-flash

###############################
# Notes
###############################
# - If you get HF error 40301 ("Only Qwen/..."), switch HUGGINGFACE_MODEL to one of the public models above.
# - If you get "Model not found: gemini-1.5-pro", set GEMINI_MODEL to gemini-1.5-flash (free keys).
# - If Gemini Vision 404 occurs, ensure GEMINI_API_URL uses `/v1` (not `v1beta`) as above.

# (Optional) NextAuth / other app secrets used by local dev — keep these if the app expects them:
# NEXTAUTH_SECRET=some-long-secret
# NEXTAUTH_URL=http://localhost:3000
